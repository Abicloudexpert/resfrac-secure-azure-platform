# Azure Functions Python worker + runtime deps.
# Pinned to compatible major ranges; the pipeline installs into .python_packages.
azure-functions==1.21.3
azure-identity==1.19.0
azure-storage-blob==12.24.0
